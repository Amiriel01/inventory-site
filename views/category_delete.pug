extends layout 

block content 

    h1 #{title}: #{category.category_name}

    if category_items.length 

        h2 Delete all items in category before attempting to delete this category.

            h3 Items 

            dl 
            each item in category_items 
                dt 
                    a(href=item.url) #{item.item_name}

    else 

        h2 Confirm Category Deletion

        h3 This change cannot be reversed!  

        form(method='POST' action='')
            div.form-group 
                input#categoryid.form-control(type='hidden', name='categoryid', required='true', value=category_id)

            button.btn.btn-primary(type='submit') Delete Category    



    